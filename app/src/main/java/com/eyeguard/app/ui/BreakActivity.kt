package com.eyeguard.app.ui

import androidx.appcompat.app.AppCompatActivity

/**
 * Break countdown is now handled entirely by OverlayManager.showBreakCountdown()
 * via a WindowManager overlay. This stub keeps AndroidManifest.xml valid and
 * the project compilable without changes to the manifest.
 */
class BreakActivity : AppCompatActivity()
